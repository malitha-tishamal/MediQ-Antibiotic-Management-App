name: mediq
description: "A new Flutter project."

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

# ------------------ Dependencies (CONSOLIDATED) ------------------
dependencies:
  flutter:
    sdk: flutter

  # Core Flutter and Utility Packages
  cupertino_icons: ^1.0.8
  
  # New: Local Storage
  shared_preferences: ^2.2.3 # Added the dependency here

  # Firebase Packages
  firebase_core: ^2.24.2 
  firebase_auth: ^4.15.2 
  cloud_firestore: ^4.14.0 # Firestore for role management

# ------------------ Development Dependencies ------------------
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0 

# ------------------ Flutter Specifics ------------------
flutter:
  uses-material-design: true

  assets:
    - assets/logo.png
